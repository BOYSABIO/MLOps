name: mlops_pipeline

docker_env:
  image: mnist-api:latest

entry_points:
  main:
    parameters:
      step:
        type: string
        default: "all"
    command: >
      python src/main.py --step {step}
